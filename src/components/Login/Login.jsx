import React from 'react';
import LoginForm from './LoginForm';

const Login = (props) => {
    return (
        <div>
            <h2>Login</h2>
            <LoginForm />
        </div>
    ) 
}

export default Login;